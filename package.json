{
  "name": "fastify-now",
  "version": "0.2.1",
  "description": "Fastify Now - structure your endpoints in a folder and load them dynamically",
  "author": "Yonatan Bendahan",
  "keywords": [
    "fastify",
    "typescript"
  ],
  "license": "MIT",
  "main": ".build/index.js",
  "types": ".build/index.d.ts",
  "repository": {
    "type": "github",
    "url": "https://github.com/yonathan06/fastify-now"
  },
  "scripts": {
    "build": "tsc",
    "prepublish": "npm test && npm run build",
    "pretest": "tsc --project tsconfig.test.json",
    "test": "ava test-build/**/*.spec.js"
  },
  "engines": {
    "node": "12.x.x"
  },
  "dependencies": {
    "fastify": "^2.13.0",
    "fastify-plugin": "^1.6.1"
  },
  "devDependencies": {
    "@ava/typescript": "^1.1.1",
    "@types/node": "^12.7.8",
    "ava": "^3.8.1",
    "typescript": "^3.8.3"
  }
}
